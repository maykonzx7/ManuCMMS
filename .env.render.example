# Copie para .env.render (não versionado) e use: source .env.render
#
# Render Dashboard → Account Settings → API Keys
RENDER_API_KEY=rnd_sua_chave_aqui

# Opcional — se a API não tiver repo conectado no Render
# RENDER_REPO=https://github.com/maykonzx7/ManuCMMS
